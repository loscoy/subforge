# 复制为 .env 后按需填写

# 管理接口口令（留空时管理接口默认锁定）
ADMIN_TOKEN=

# 远端 MCP Bearer token（留空 = 禁用远端 MCP）
MCP_TOKEN=

# Agent：任意兼容 OpenAI Chat Completions 的服务
# 例：OpenAI 官方 / 本地 ollama(/v1) / 各类中转
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-xxxx
OPENAI_MODEL=gpt-4o-mini
