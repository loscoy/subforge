# 复制为 .env 后按需填写
#
# 这里只放「引导配置」——把服务跑起来所需的东西。
# 模型、联网工具、远端 MCP 口令属于运行时设置，存数据库、在 Web「设置」页里配，
# 改完即时生效不用重启（旧版的 OPENAI_* / MCP_TOKEN / AGENT_WEB_* 已废弃）。

# （遗留）旧管理口令：管理鉴权已改为账号密码登录（首次打开网页时创建）。
# 仅存量部署升级时保留——建号需验证它（防抢注），建号后可删除本行。
ADMIN_TOKEN=

# 加密数据库里密钥字段（模型 API Key、MCP 口令等）的主密钥。
# 任意足够长的随机串即可，例如 `openssl rand -base64 32`。
# 未设时密钥存不进去，Agent 与远端 MCP 保持关闭；
# 换掉它会让已存的密钥全部解不开，需要在设置页重填。
SETTINGS_KEY=
